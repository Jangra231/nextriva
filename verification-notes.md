# Verification Notes

- Capability workspace repair: active CSR grant `840001` correctly contained `CSR_BRIEF_SUBMIT`, was active through 31 October 2026, and was national despite retaining optional locality planning metadata. The authorization evaluator previously treated those optional fields as mandatory for a national grant, blocking an enabled workspace action. National grants now authorize selected functions across supported territories, while state/district/city/zone/ward grants retain exact scope enforcement. The focused authorization suite passed (3 tests) and TypeScript passed. Read-only preservation counts remain 5 users, 36 events, 7 registrations, 1 capability grant, and 5 selected-function links.

- Non-mutating organizer browser verification on draft `150005`: the native **Add New Question** control changed only the step-six URL to add a blank third question, while the two saved questions remained intact and no Save Questions action was submitted.
- Non-mutating organizer browser audit on the same draft: step navigation, manual-location entry, and the server-safe **Browse approved venue directory** link rendered their expected controls and live availability calendar without submitting or changing the event.

- The public event banner renders the QR image above the direct `Download QR image` link, with poster and sharing actions remaining visible below the QR card.
- The printable poster route returns successfully and contains the neutral text `Registration is securely embedded in this QR code.` without exposing `localhost` or the Manus preview hostname as visible poster text.
- The public event header and organizer Cover Image view now render a full-width QR registration panel, with guidance and QR side by side and the four download/share actions aligned in one row below.
- The standalone administrator account signs in through `/admin/login` and reaches a protected read-safe console showing platform accounts, events, registrations, payments, and reports without exposing data-changing controls.
- The administrator Events and Payments views load existing platform records, ticket capacity, and payment status after authentication; neither view presents edit, approval, rejection, deletion, or bank-detail controls.
- The participant workspace now exposes visible My Bookings, Following, Explore Events, and Account controls. Its account menu includes profile editing, role selection, participant links, and Logout; logout clears the session and returns to the public homepage with only the Login | Signup entry shown.
- The Account control remains visible with a text label rather than relying on an avatar-only affordance, including at the participant workspace breakpoint.
- The organizer workspace retains its own Reports, Attendees, My Events, and Promotions navigation while using the same visible Account menu and Logout control as the participant workspace.
- The upgraded administrator overview shows guarded master scope, platform health, and audit activity. Its protected XLSX export downloaded successfully with events, registrations, payment status, summary metrics, and audit-log worksheets.
- The protected administrator PDF export downloaded successfully and was validated as a readable A4 report with platform metrics and event-performance content.
- The administrator Reports view now renders weekly activity from live registration data, with interactive controls for registrations, gross ticket value, and platform-fee totals, plus a moderation-mix visualization and protected XLSX/PDF actions.
- The approval workflow migration was verified as non-destructive: existing records retain their original draft/live lifecycle values while receiving default moderation and platform-fee fields.
- Live administrator report verification showed current ticket capacity, sold-ticket count, gross value, recorded platform fees, seven-day activity bars, moderation totals, and both protected export actions in the authenticated console.
- The administrator Events view was rendered against current records and confirmed that every row exposes a guarded decision selector, fee percentage input, organizer-guidance field, and required MASTER confirmation; no moderation action was submitted during verification.
- Organizer approval now creates one complimentary confirmed first-participant record with generated booking and registration IDs, the organizer public ID, and the first active ticket when available. Repeated approval checks reuse the same record without a duplicate or an extra capacity increment.
- The database-backed organizer-participation test passed and its temporary event and user records were confirmed removed after completion; the full suite now has 54 passing tests and the production build succeeds.
- Attendee management now marks the organizer’s automatic first-participant record with an inline Organizer badge. Protected CSV exports include a Participation Role column while retaining the generated user ID, booking ID, registration ID, and ticket details.
- A successful first approval sends the organizer a safe SMTP-ready participation email containing the permanent user ID, event ID, booking reference, and ticket details; repeat approvals do not send a second email because no new organizer participation record is created.
- The identifier model now uses a permanent unique user ID for every account and a permanent unique event ID for every event. Existing events were backfilled as `EVT-<event primary key>` without changing their existing records; new participations do not receive registration-specific IDs, and organizers can verify confirmed participation with the user-ID/event-ID pair. Database checks confirmed 3 distinct user IDs for 3 users and 35 distinct event IDs for 35 events. TypeScript, 54 automated tests, and the production build pass.
- After the event-ID migration, the managed preview successfully rendered the public event-list view. The temporary external-browser availability page was isolated from the managed preview and did not indicate an application error.
- The venue directory migration created only the new approved-venue table and nullable precise-location fields on events. No existing event records were modified. The administrator venue integration test created, updated, retired, audited, and then removed its temporary venue, test account, and audit data; post-test database checks confirmed zero temporary venue records, users, and orphaned venue audit records.
- Organizers can now select an active administrator-approved venue or enter city, venue, zone, ward, sector, area, address, indoor/outdoor setting, and optional browser-captured GPS coordinates. Public event pages use saved address and coordinates to display precise details and an exact map-directions link. TypeScript, 58 automated tests, and the production build pass.
- The extended venue workflow adds optional capacity, accessibility status, and accessibility notes to approved venues and event location snapshots without changing existing records. The CSV parser validates required columns, GPS ranges, capacity range, and duplicate rows before persistence; the guarded import action supports up to 500 rows and produces row-level error feedback. Database-backed tests confirmed duplicate-safe imported venue creation and update behavior, audit logging, and cleanup of all temporary test records.
- The organizer manual-location editor now includes an authenticated interactive map. Organizers can drag the location pin or click the map to update latitude and longitude before autosave. The map configuration endpoint was verified to return the configured proxy script URL. The administrator overview now exposes approval, payment, venue-readiness, revenue, platform-fee, upcoming-event, and audit activity queues. TypeScript, 61 automated tests, and the bounded-memory production build pass.
- The public discovery page continues to render live event cards with registration capacity metadata. The new card-level venue-capacity and accessibility labels are covered with server-rendered component tests because current preserved live-event records have no venue snapshots.
- The authenticated Administrator Console Venue directory view renders the new `Export complete directory` action beside the CSV template download. It remains inside the guarded administrator workspace and does not perform a mutation.
- The protected export action was downloaded from the authenticated console and confirmed in browser download history as `fitizen-venue-directory.csv`.
- Organizer venue selection now filters client-side by matching text, zone, ward, minimum capacity, and accessibility without changing directory records. The full validation pass completed with TypeScript, 25 Vitest files / 64 tests, and the bounded-memory production build passing; expected mocked scheduler error logs remained non-failing test fixtures.
- Organizers can now use their current browser location for 5, 10, 25, 50, or 100 km approved-venue radius searches. Location stays in the browser session and is not stored with saved presets; each organizer can save up to 20 named, privately owned filter presets and can only remove their own records.
- Public discovery supports an Accessible venues-only filter in both the homepage controls and Events search page. Browser verification confirmed the selected public filter and responsive layout; database-backed tests confirmed accessible snapshot filtering and preset ownership cleanup. TypeScript, 27 Vitest files / 69 tests, and the bounded-memory production build pass without persistent test data.
- Live map diagnosis confirmed `/api/maps/config` returns a usable Manus Maps proxy script URL without requesting a user key. A direct script-evaluation attempt on the JSON endpoint itself is not a usable map-page test context, so map-script verification continues from an application page after the loader repair.
- A second script-evaluation attempt from the application page was blocked by the browser automation runtime before completion rather than by an application response. The repaired map component therefore includes explicit retry and error messaging, while endpoint and build validation remain the automated evidence.
- Approved-venue booking is now enforced on both autosave and submitted location saves: an event cannot claim a venue held by another non-completed, non-removed event. Organizer location selection shows live reserved-venue messages, disables held options, and still runs the authoritative server check to prevent concurrent or bypassed selections.
- Organizers can mark an ended live event completed from My Events, which releases its approved venue. Database-backed coverage confirmed a requester is blocked while the holder is active and released immediately after the guarded completion action; all temporary users, events, and venue data were removed by test cleanup. TypeScript, 29 test files / 71 tests, and the bounded-memory production build pass.
- The administrator Venue directory now visibly contains the three user-requested, clearly labelled Sample records: Riverfront Activity Ground, Civic Indoor Hall, and Lake Park Amphitheatre. They were inserted once through the audited duplicate-safe venue helper; no existing venue records were altered.
- The organizer location step now uses a full-width approved-venue directory selector, includes a clear manual-location fallback, and provides a responsive month calendar for any chosen directory venue. Reserved dates show their holding event, while open dates are explicitly marked available.
- Administrators can now apply a MASTER-confirmed exceptional release with a mandatory explanatory note. The action clears only the active event-to-directory reservation, records before/after audit state, and triggers watches. Organizers can watch a reserved venue and receive a private in-app inbox alert when completion or an administrator release makes it available. Migration 0019 added only availability watch and notification tables. Browser verification confirmed the authenticated admin controls and the three sample venues; TypeScript, 29 Vitest files / 72 tests, and the bounded-memory production build pass.
- The Create Event location step was corrected to be directory-first for fresh drafts when approved venues exist. It now shows an explicit venue count, a full-width directory browsing panel, clear confirmed-versus-undecided and directory-versus-manual choices, and a visible manual-entry fallback. Existing manual location details still open in manual mode to avoid hiding a draft’s saved work.
- A clean 2.3 MB source archive was created with `SETUP.md`, migration 0019, and `package.json`; it excludes dependencies, Next build output, runtime logs, local environment files, database files, and Manus metadata. `SETUP.md` documents environment variables, migrations, venue samples, local run, testing, production build, and deployment checks. TypeScript, 29 Vitest files / 72 tests, and the bounded-memory production build pass.
- Browser verification for the latest Create Event correction confirmed that the approved-venue dropdown is now rendered at the top of Step 2 and immediately lists all three administrator-created sample venues. The isolated manual-location check also rendered its coordinates and map canvas; the map remained in its loading state during the first browser observation, so proxy initialization is being investigated before final delivery.
- The Create Event Step 2 directory picker was further simplified after rendered browser inspection: it now appears before availability, calendars, filters, saved presets, or radius controls and lists every active approved venue without a filter dependency. Manual browser verification confirmed three directory options are directly visible in the native selector.
- The configured Maps proxy endpoint returns a script URL, but the returned script responded `403` in browser and sandbox checks. The manual location editor therefore now renders an interactive OpenStreetMap coordinate preview from first paint, while retaining browser GPS capture, typed coordinates, a direct coordinate-map link, proxy retry, and the native draggable/clickable map pin whenever the proxy accepts loading. The isolated verification draft was removed after browser checks. TypeScript, 29 Vitest files / 72 tests, and the bounded-memory production build pass.
- The Create Event Step 2 source controls are now server-safe links as well as enhanced client controls. In a real isolated organizer draft, clicking **Browse approved venue directory** navigated to the directory branch and immediately rendered the full native venue picker below it with all three existing labelled Sample venues; no existing record was changed.
- The selected venue view now shows a responsive visual month calendar and an explicit selected-day planning panel with eight 06:00–20:00 time slots. Browser verification confirmed the date buttons expose Available/Reserved state and the selected sample venue rendered all eight available slots; the full-event reservation guard remains authoritative.
- Organizers can submit detailed manual venue proposals, see their private status, and administrators receive a MASTER-confirmed review queue offering approval, request-changes, and rejection with organizer-facing guidance. Approval creates an active directory venue, links the request event, and records audit state. Browser verification submitted and displayed a pending isolated request; the temporary request and draft were subsequently removed.
- The database-backed venue-approval-request integration test verifies organizer scoping, approval, active directory-venue creation, event linkage, and review note persistence with automatic test cleanup. Final validation passed: TypeScript, 30 Vitest files / 73 tests, and the bounded-memory production build.
- Migration `0021_sudden_captain_flint.sql` was reviewed and applied as a non-destructive `users.role` enum extension for the new `mcd` authority role; no existing account record was modified. Master administrators can provision MCD accounts through the Accounts view with `MASTER` confirmation, password validation, and an append-only `mcd.account_created` audit entry.
- The requested MCD demo account was created through that administrator workflow: `mcd@fitizen.local` (MCD Demo Authority). Browser verification confirmed its dedicated `/mcd/login` route, the MCD monitoring dashboard, full event table, ward/zone/city activity, organizer monitoring, eligible-location view, and transparent deferred health-screening, CSR, and implementation-agency baselines.
- MCD accounts can record only auditable event lifecycle reviews: approve or request changes for submitted events, and freeze or suspend approved events. Account, payment, venue-directory, and master administrator routes remain unavailable; browser verification confirmed an MCD session redirects from organizer pages to `/mcd` and receives administrator-access denial at `/admin`.
- The protected MCD MIS XLSX download completed successfully as `fitizen-mcd-mis-2026-08-22.xlsx`; the workbook contains Summary, Ward activity, Events, and Eligible locations sheets. The companion PDF route is available. Exports do not invent health-screening, CSR, or implementation-agency metrics that are not currently captured.
- Database-backed MCD integration coverage verifies audited MCD account provisioning and event approval while preserving platform fees, with cleanup of all temporary test records. Final validation passed: 31 Vitest files / 76 tests and the bounded-memory production build.
- Migration `0022_steady_santa_claus.sql` was reviewed and applied as an additive CSR extension: the `csr` user role, CSR company profiles, budgets, master-registered implementation agencies, and auditable sponsorship commitments. Existing platform records were not modified.
- Master administrators can create CSR sponsor accounts only with a `MASTER` confirmation, company/contact profile, password validation, and an append-only `csr.account_created` audit entry. The requested demo account was provisioned through this UI as `csr@fitizen.local` for Fitizen CSR Demo Foundation.
- The dedicated `/csr/login` and `/csr` workspace display the corporate profile, budgets, scoped funding proposals, approved funded events, recorded participation, impact-by-city, and protected XLSX/PDF CSR reports. Browser verification confirmed the dashboard renders the explicitly separate funding-versus-organiser explanation and that an XLSX report downloaded successfully as `fitizen-csr-report-2026-08-22.xlsx`.
- A sponsorship proposal must select a CSR budget, live event, activity, approved ward/zone/location, and master-registered implementation agency. Sponsor ownership checks reject an attempt to fund the CSR account’s own event; master approval checks the remaining budget, records commitment/audit state, and never alters the event organiser.
- CSR sessions are redirected away from organizer workflows. Master administrator, payment, venue-directory, and user-management controls remain unavailable to the CSR role. MCD monitoring now reports approved CSR commitment totals without inferring payment settlement, delivery milestones, health screening, or verified social outcomes.
- Database-backed CSR integration coverage verifies account/profile provisioning, budget creation, proposal submission, MASTER approval, committed-budget arithmetic, preserved organiser ownership, CSR impact aggregation, audit logging, and deletion of all temporary test accounts, events, venues, agencies, budgets, sponsorships, categories, and audit data. Final validation passed: TypeScript, 32 Vitest files / 78 tests, protected browser reporting, and the bounded-memory production build.
- CSR sponsors can now select **existing live events** directly from the Sponsor events view, alongside the CSR budget, master-approved funding location, implementation agency, amount, and purpose. Browser verification confirmed the demo sponsor sees the existing events `Secure Session Publish Test` and `Sort Verification Community Walk`, all three approved venue choices, the demo budget, and a clear no-agency blocker rather than a hidden or non-functional workflow.
- Sponsorships now follow the audited lifecycle `draft → submitted (awaiting MCD) → mcd_approved (awaiting MASTER) → approved/rejected`. An MCD account must type `MCD` to clear or return a proposal; a master administrator must type `MASTER` to approve or return only an MCD-cleared proposal. The final master approval alone increments the selected CSR budget commitment; every MCD and master decision is recorded without changing the event organiser.
- Updated database-backed CSR coverage verifies an existing-event funding draft, sponsor ownership protection, blocked master approval before MCD clearance, MCD clearance, unchanged budget commitment before the final decision, final MASTER commitment, preserved organiser ownership, audit logging, and complete temporary-data cleanup. Final validation passed: TypeScript, 32 Vitest files / 78 tests, browser MCD/CSR views, and bounded-memory Next.js production build.
- Removed direct Vite dependencies and the obsolete `vite.config.ts` from the active Next.js project. The clean 472 KB `fitizen-india-rebuild-nextjs-csr-sequential-approval.zip` archive contains the active Next.js `app`, migrations, configuration, setup, tests, and documentation only; archive inspection confirms it excludes legacy Vite config/client/server/shared source, dependencies, build output, logs, local environments, and Manus metadata.
- Migration `0024_strange_natasha_romanoff.sql` was applied as an additive CSR-request change. It preserves the historic implementation-agency and direct-sponsorship data tables while the current CSR workspace and administrator controls use only the new agency-free sponsorship-brief records.
- The active CSR workflow now offers exactly two non-browsing routes—interest in an existing matched event or a future-event brief—and captures event type, intended audience, city/zone/ward preference, timing, amount, capacity, accessibility, requirements, and success indicators. Browser verification with the isolated CSR account confirmed both route choices and all brief fields render while no general platform-event list is exposed.
- CSR brief review is master-administrator-only: submitted records can be approved for matching, returned for additions with visible guidance, or rejected with a required reason. Returned drafts are owner-scoped and prefill a revision form before resubmission. Only a separate MASTER-confirmed assignment to one live organiser-owned event commits the selected CSR budget; event ownership is unchanged.
- Retired the active implementation-agency controls, direct sponsorship actions, MCD CSR sponsorship-review control, and MCD review navigation. MCD now has read-only visibility of administrator-assigned CSR-supported activity only; the protected MCD MIS report uses the same assigned-event boundary.
- Browser verification confirmed the CSR demo account sees the renamed Sponsorship briefs navigation, the two-route agency-free form, detailed requirement fields, and an empty assigned-event-only view. The administrator CSR workspace shows only request governance and assignment queues with no agency controls; no persistent demo records were changed during visual validation.
- The rewritten database-backed CSR lifecycle test covers draft creation, administrator additions request, owned revision and resubmission, approval pending assignment, one live-event match, exactly-once budget commitment, preserved organiser ownership, required rejection reasons, sponsor isolation, audit logging, and automatic cleanup. Final validation passed: TypeScript, 32 Vitest files / 79 tests, and a bounded-memory Next.js 16.3.1 production build.
- Workflow UI refinement adds semantic status badges with visible state dots for live/approved, awaiting review, draft, additions requested, and rejected or suspended workflows. Returned and rejected CSR requests now use warning and critical badge treatments rather than the generic draft style.
- Shared hover and focus feedback now applies to workflow panels, cards, table rows, navigation, and guarded control groups. The new route loader provides an accessible animated workspace-loading state, while every non-essential animation has a `prefers-reduced-motion` fallback.
- Administrator master controls, administrator CSR review and event-assignment controls, and MCD event review controls now show a spinner, pending label, and disabled state during server-action submission. Browser review confirmed the populated administrator event workflow renders the improved lifecycle badges and guarded action controls without submitting any changes.
- Added `workflow-ui.test.ts` to protect status-tone, hover, reduced-motion, pending-submit, and route-loading contracts. Final validation passed with TypeScript, 33 Vitest files / 81 tests, the bounded-memory Next.js production build, and no workflow data mutations.
- **Stage 0 — staged capability migration baseline:** The uploaded staged migration document was fully analyzed and converted into `STAGED_MIGRATION_ROADMAP.md` and `STAGE_0_MIGRATION_BASELINE.md`. The Stage 0 report inventories the Next.js 16.3.1 App Router structure, custom JWT session, current `user`/`admin`/`mcd`/`csr` role model, numeric and public IDs, MySQL/TiDB Drizzle schema and migrations through `0024`, active routes/APIs, direct role guards, CSR/MCD compatibility surfaces, test/build commands, backup/export controls, and feature-flag-first rollback procedures.
- Stage 0 made documentation-only additions. No authentication behavior, database schema, migrations, data records, role values, routes, or production workflows were changed. The project baseline passed `pnpm check`, 33 Vitest files / 81 tests, and a bounded-memory Next.js 16.3.1 production build; the development service was restarted successfully afterward.
- **Stage 1 — Local Authority terminology compatibility:** Migration `0025_medical_alex_wilder.sql` additively creates and duplicate-safely seeds `authorityTerminologyMappings` for historic `mcd`, `bmc`, and `mcd/bmc` values. It was applied and deliberately re-run successfully; existing user records retain the stored `mcd` role and current role counts remain unchanged.
- Canonical Local Authority pages, login, MIS API, navigation, administrator provisioning, reports, action messages, and audit codes now use Local Authority wording. `/mcd`, `/mcd/login`, and `/api/exports/mcd` remain safe compatibility redirects. The `FITIZEN_LOCAL_AUTHORITY_TERMINOLOGY=false` feature flag provides a non-destructive route rollback without rewriting account data or audit history.
- Browser verification confirmed that `mcd@fitizen.local` authenticates through `/local-authority/login`, the active Local Authority workspace renders without legacy user-facing terminology, and `/mcd?view=reports` redirects to the matching canonical reports view. Focused compatibility tests, the full suite, type check, and bounded-memory production build passed; the full suite now has 34 Vitest files / 84 tests.
- **Stage 2 — additive account and capability foundation:** Migrations `0026_workable_saracen.sql` and `0027_aspiring_ma_gnuci.sql` add `userAccountProfiles`, `legacyAccountCapabilityMappings`, and `accountMigrationRecords`. Existing accounts retain their numeric IDs, public IDs, credentials, current sessions, `users.role` values, ownership, events, registrations, sponsorship records, and historic audit records. Database verification confirmed five legacy users, five profiles, five migration records, and only the existing `mcd → LOCAL_AUTHORITY` and `csr → CSR` active compatibility mappings.
- The current `admin` role maps to `PLATFORM_ADMIN`; all other retained roles map to `USER`. The profile view now uses explicit User Profile or Platform Admin Profile terminology from the additive profile row. Capability mappings are compatibility metadata only and do not grant a new menu, function, scope, or server-side action ahead of the Stage 3/4 grant-and-authorization work.
- `FITIZEN_ACCOUNT_PROFILE_ROUTE_MIGRATION` defaults to disabled, retaining `/dashboard/profile`; `/account/profile` is an intentionally guarded canonical route. Browser verification with the Local Authority session confirmed the disabled canonical profile route safely falls back through retained routing without exposing an unrelated profile context. The Local Authority dashboard has a dedicated loading state, hover/focus treatment, review-control pending styling, animated report-action feedback, and reduced-motion safeguards. No authority review was submitted.
- Stage 2 focused coverage verifies permanent-user preservation, USER/PLATFORM_ADMIN profile mapping, historic-but-inactive capability mapping retention after legacy-role changes, duplicate-safe migrations, guarded route content, and Local Authority UI hooks. Final validation passed with TypeScript, 35 Vitest files / 87 tests, and the bounded-memory Next.js 16.3.1 production build.

- **Stage 3 — capability catalog, applications, scoped grants, and audits:** Migrations `0028_ambitious_adam_warlock.sql` and `0029_purple_electro.sql` were applied additively. They create the capability catalog/application/grant/audit model and then add mandatory/optional, dependency, and sensitive-data metadata. The catalog contains six active records—Local Authority, CSR sponsorship, venue stewardship, event hosting, district delivery, and state programme stewardship—without modifying legacy users, roles, sessions, events, ownership, CSR data, registrations, or historic audit records.
- Stage 3 lifecycle mutations now use database transactions. Draft save/revision, submission, return/rejection, approval plus selected-function time-bound grant creation, and grant-status updates commit their corresponding application/function/grant/capability-audit/administrator-audit records atomically. The focused integration test covers a scoped Noida lifecycle through returned revision, approval, selected-function grant, suspension, audit evidence, mandatory-function seed coverage, and complete temporary-data cleanup.
- Browser verification confirmed a standard account sees six searchable/filterable catalog entries, required/optional labels, sensitive-data declarations, dependency text, and preselected mandatory functions in the State programme request form. A standard account was denied the administrator capability route; the administrator Capability Governance view displayed six active records, zero-state lifecycle tables, and the explicit Stage 4 non-enforcement boundary. No browser-side lifecycle mutation was submitted.
- `FITIZEN_CAPABILITY_CATALOG_ENABLED` now provides the Stage 3 operational rollback path. It defaults enabled; the exact value `false` hides new capability navigation and returns direct applicant/admin capability views to retained workspaces while preserving all additive Stage 3 data. A dedicated flag test protects this behavior.
- Final Stage 3 validation passed with TypeScript, **37 Vitest files / 90 tests**, and the bounded-memory Next.js 16.3.1 production build. Post-suite database verification reported `users=5;profiles=5;applications=0;grants=0;audits=0;stage3TestUsers=0`; retained legacy role counts remained `user` 2, `admin` 1, `mcd` 1, and `csr` 1.
- The fresh `fitizen-india-rebuild-nextjs-stage3-capability-catalog.zip` archive was rebuilt after all Stage 3 documentation changes and inspected. It is 572 KB with 329 source entries and includes `package.json`, `SETUP.md`, Stage 3 documentation, and migrations `0028`/`0029`; it excludes dependencies, Next.js build output, legacy Vite client/server/shared source, runtime logs, Manus metadata, local secrets, and database files.

- **Stage 4 — central authorization, Local Authority cutover, grant expiry, and catalog usability:** Added a centralized server-side decision service that evaluates active status, start/end time, selected function, function dependencies, and city/zone/ward scope. Its first and only migrated mutation is Local Authority event review with `LOCAL_AUTHORITY` + `LA_EVENT_REVIEW`; no CSR, organizer, administrator, monitoring, MIS-export, or other legacy path was silently rerouted.
- `FITIZEN_CAPABILITY_AUTHORIZATION_ENFORCEMENT` defaults disabled and enables enforcement only at the exact lowercase value `true`. With the default compatibility setting, the retained `mcd` role continues to authorize Local Authority review. With enforcement enabled, a selected-function, time-valid, territory-matching grant is required and successful review writes `capability.execution_authorized` evidence. The dedicated Local Authority migration-grant form requires MASTER and preserves the stored legacy role.
- The Stage 4 governance view displays active catalog, submitted, active-grant, and due-expiry metrics; provides an audited MASTER-confirmed direct Local Authority migration grant; and provides an idempotent audited MASTER-confirmed batch expiry operation. A past-end active grant is already ineffective for authorization before batch expiry persists its `expired` status.
- Browser validation showed all six advanced catalog controls and applied the optional-function filter from six to five matching capabilities while preserving query state in capability request links. The administrator capability page rendered the migration-grant and zero-state expiry controls. Administrator access was denied at the Local Authority route; the retained Local Authority demo session rendered existing monitoring data and the Stage 4 compatibility-mode event-review notice. No browser mutation was submitted.
- Focused Stage 4 tests covered strict flag behavior, exact scope/function/date evaluation, compatibility-mode access, enforced denial without a grant, selected-function city-scoped authorization, audit evidence, and batch expiry. The complete regression suite passed with **39 Vitest files / 94 tests**; TypeScript and the bounded-memory Next.js 16.3.1 production build passed. Final database verification reported `users=5;profiles=5;roles=admin:1,csr:1,mcd:1,user:2;catalog=6;stage4Users=0;stage4Events=0;stage4Grants=0;stage4Audits=0;dueActive=0`.
- The final rebuilt `fitizen-india-rebuild-nextjs-stage4-authorization-expiry.zip` archive contains 272 source entries. It contains the active Next.js source, `package.json`, `SETUP.md`, Stage 4 authorization documentation, and Stage 4 authorization tests; archive inspection confirmed it excludes dependencies, Next.js build output, legacy Vite client/server/shared source, runtime logs, Manus metadata, local environment files, and database files.

- **Stage 5 — selective Local Authority MIS/CSR authorization, grant reminders, and responsive capability layout:** The centralized decision service now has independent disabled-by-default rollout controls for Local Authority MIS export and selected CSR functions. `FITIZEN_CAPABILITY_MIS_EXPORT_ENFORCEMENT=true` requires a current national `LOCAL_AUTHORITY` + `LA_MIS_EXPORT` selected-function grant for valid XLSX/PDF Local Authority MIS downloads. `FITIZEN_CSR_CAPABILITY_AUTHORIZATION_ENFORCEMENT=true` requires a current scope-matching `CSR_BRIEF_SUBMIT` grant for CSR brief submission and a national `CSR_IMPACT_VIEW` grant for valid CSR XLSX/PDF impact downloads.
- Administrator Capability Governance now provides a MASTER-confirmed CSR migration-grant form for retained `csr` users. It validates active selected functions, mandatory functions, dependencies, scope, dates, and reason; it does not alter the stored `csr` role, CSR profile, budget, request, assignment, event ownership, or historic CSR data. Successful grant-enforced brief submissions and exports append `capability.execution_authorized` evidence only after a valid export format is accepted.
- A read-only Grant reminders widget now appears in the capability, CSR, and Local Authority overview dashboards. It shows only active grants that have started and end in the next 30 days; the final active-grant/due-expiry database counts were zero, so browser verification correctly showed its empty state. It introduces no scheduler, automatic status change, notification send, or synthetic alert data. The retained MASTER-confirmed Stage 4 expiry operation remains responsible for persisting due active grants as expired.
- Responsive hardening added minimum-width, wrapping, long-text, card, filter, scope-grid, governance-control, and small-screen single-column rules. Browser verification confirmed the Local Authority alert widget sits cleanly between the page header and metrics, while the participant dashboard preserves navigation and renders grant reminders, all six filters, and all six catalog cards without desktop overlap. The unauthenticated narrow preview returned the expected protected blank shell and was not interpreted as a route failure.
- Focused Stage 5 coverage validates disabled compatibility access, Local Authority MIS selected-function authorization, CSR brief denial without a grant, city-scoped CSR selected-function authorization, capability execution audit evidence, the near-expiry alert query, and complete cleanup. The complete regression suite passed with **40 Vitest files / 96 tests**; TypeScript and the bounded-memory Next.js 16.3.1 production build passed. Final database verification reported `users=5;profiles=5;roles=admin:1,csr:1,mcd:1,user:2;catalog=6;activeGrants=0;dueActive=0;stage5Users=0;stage5Grants=0;stage5Requests=0;stage5Audits=0`.
- The final `fitizen-india-rebuild-nextjs-stage5-selective-authorization-alerts.zip` archive was inspected with 277 source entries. It includes the active Next.js source, `package.json`, `SETUP.md`, the Stage 5 completion document, the grant-alert component, and the Stage 5 integration test; it excludes dependencies, Next.js build output, legacy Vite client/server/shared source, runtime logs, Manus metadata, local environment files, and database files.

- **Stage 6 — administrator approvals and owner-scoped decision notifications:** The Stage 6 document gate was reconciled against Stages 3–5. Existing MASTER-confirmed application review already allows fewer optional functions than requested while rejecting unrequested, inactive, missing-required, and dependency-incomplete function selections; it validates dates/scope/reason and records capability plus administrator audit evidence in the same transaction.
- Migration `0030_gorgeous_sprite.sql` additively creates `capabilityDecisionNotifications`, an indexed, owner-scoped private inbox with optional application/grant references, decision kind, title, reason body, action route, read timestamp, and creation time. It does not change legacy users, roles, public IDs, sessions, event/CSR ownership, grants, or historic audits.
- Application return/rejection/approval, grant status changes, batch expiry, and direct Local Authority/CSR migration grants now append a recipient notification inside their existing transaction when `FITIZEN_CAPABILITY_DECISION_NOTIFICATIONS_ENABLED` is not the exact value `false`. The applicant capability page shows unread notices above reminders; mark-read is owner-scoped by an ID-and-user predicate. Setting the flag to `false` hides the inbox and stops future notifications while retaining all lifecycle behavior and stored evidence.
- Browser verification inserted one isolated participant decision notice, confirmed its responsive wrapped card, non-overlapping Review details/Mark read controls, and owner-scoped mark-read redirect, then removed it. Capability grant guidance now accurately describes Stage 4–5 selective rollout enforcement instead of implying every grant is merely informational.
- Focused lifecycle coverage verified requested revision, selective approval, suspension, multiple decision-notification types, audits, and cleanup; the flag test covers default-enabled/exact-disable semantics. Final validation passed with **40 Vitest files / 97 tests**, TypeScript, and the bounded-memory Next.js 16.3.1 production build. Final database verification reported `users=5;profiles=5;roles=admin:1,csr:1,mcd:1,user:2;catalog=6;decisionNotifications=0;stage3Users=0;stage3Applications=0;stage3Grants=0;stage3DecisionNotifications=0;dueActive=0`.
- The inspected `fitizen-india-rebuild-nextjs-stage6-admin-approval-notifications.zip` archive contains 284 active-source entries, including `package.json`, `SETUP.md`, migration `0030_gorgeous_sprite.sql`, the Stage 6 completion document, and the private capability decision-inbox component. Archive inspection confirms it excludes dependencies, Next.js build output, legacy Vite client/server/shared source, runtime logs, Manus metadata, local environment files, and database files.

- **Stage 6 enhancement and Stage 7 — private inbox controls and active capability workspace switcher:** The decision inbox now supports a closed owner-scoped outcome filter, bulk mark-read of only the signed-in recipient’s matching unread notices, individual mark-read, unread counts in the standard-user navigation/account menu, and short reduced-motion-safe transitions. These controls do not alter the underlying application, grant, selected-function, audit, or authorization state.
- Stage 7 introduces `FITIZEN_CAPABILITY_WORKSPACE_SWITCHER_ENABLED`, enabled unless its value is exactly `false`. It derives user workspaces live only from grants that belong to the current account profile, are active and current, join to an active capability, and retain at least one active selected function. The account-menu switcher and **Workspaces** navigation item appear only when this derivation yields an entry. The feature does not create workspace records, infer access from Stage 2 mappings, grant execution capability, or alter retained Local Authority/CSR role workspaces.
- Every `/dashboard/workspaces/[capabilityCode]?grant=[grantId]` request is revalidated server-side for safe input, profile ownership, active/current grant, active catalogue capability, exact code match, and selected active functions. Wrong capability codes, fake grants, foreign grants, suspended grants, expired grants, future grants, inactive capabilities, and functionless grants resolve to no context and redirect to the workspace index. Setting the feature flag to exact `false` hides the surfaces and safely routes the index to the retained user workspace without changing grants, roles, selected functions, or audits.
- Focused Stage 7 coverage created isolated accounts, notices, applications, grants, and audit evidence; it proved recipient-only filtered bulk read, unread counting, qualifying workspace derivation, wrong-code/fake-grant rejection, and suspended-grant withdrawal. Cleanup verification returned `stage7Users=0;stage7Notifications=0;stage7Applications=0;stage7Grants=0;stage7Audits=0`. Desktop browser validation confirmed the authenticated participant workspace zero state and capability dashboard have stable, non-overlapping layout. The isolated screenshot runner did not inherit the authenticated session at narrow width, so its blank protected views were correctly treated as an authentication limitation rather than UI proof; responsive source rules use min-width-safe grid cells and wrapping metadata.
- Final regression validation passed with **41 Vitest files / 100 tests**, TypeScript, and the bounded-memory Next.js 16.3.1 production build. Final preservation verification reported `users=5;profiles=5;roles=admin:1,csr:1,mcd:1,user:2;catalog=6;activeGrants=0;decisionNotifications=0;stage7Users=0;stage7Applications=0;stage7Grants=0;stage7Notifications=0`.
- The inspected `fitizen-india-rebuild-nextjs-stage7-workspace-switcher.zip` archive contains **358** entries, including Stage 7 routes, focused integration coverage, `package.json`, `SETUP.md`, the Stage 7 inventory, and the combined completion document. Archive exclusion verification found no dependencies, Next.js build output, legacy Vite client/server/shared source, runtime logs, Manus metadata, local environment files, or database files.

- **Workspace personalization and grant-usage reporting:** Added the owner-scoped `userWorkspacePreferences` record and `/dashboard/workspaces/preferences`. A standard user can save participant, organizer, or one live account-owned capability grant as the normal-login landing route. Explicit safe `returnTo` values retain precedence. A selected capability preference is validated at save and login; inactive, expired, suspended, foreign, catalog-disabled, or functionless grants fall back safely to My Bookings without becoming an authorization source. The generic workspace page also now shows a read-only audit summary for that exact grant, while the administrator Capability Governance console shows selected functions, effective status, authorized-action count, latest evidence, and the retained MASTER-protected grant controls.
- **Stage 8 — CSR capability workspace:** Migrations `0031_many_tomas.sql` and `0032_cuddly_vengeance.sql` are additive. They add workspace preferences; public identifiers for new CSR requests; separate CSR capability sponsorship, assignment, and future-event-concept tables; and a nullable `capabilityGrantId` link that deliberately separates Stage 8 requests from historic legacy CSR requests. Existing users, roles, profiles, budgets, legacy requests, events, ownership, registrations, and audit history remain unchanged.
- A selected-function, time-valid `CSR_SPONSORSHIP` grant now enables `/dashboard/workspaces/CSR_SPONSORSHIP?grant=[grantId]` for a standard account. Every setup, budget, request, submission, assignment, participant, and activity operation validates the exact active grant, ownership, scope, and selected function server-side. The Stage 8 workspace neither grants a legacy CSR role nor includes legacy CSR request rows. `CSR_BRIEF_SUBMIT`, `CSR_ASSIGNED_EVENT_VIEW`, `CSR_ASSIGNED_PARTICIPANT_VIEW`, and `CSR_FUNDING_TRACK` are independently selected active functions with dependency metadata.
- The CSR user receives a private decision notification for capability request review/assignment. MASTER administrators remain the sole review, assignment, and funding-record authority. Each capability request, sponsorship, assignment, and future-event concept receives a generated `CSR-REQ`, `CSR-SPN`, `CSR-ASN`, or `CSR-CON` public identifier. An assignment picks exactly one live organizer-owned event or one planning-only future-event concept. The latter does not represent an event and cannot be used for registration, QR, attendance, health information, or funding recording. A real assigned event may expose only the administrator-approved closed participant-field set; sensitive profile, address, identity, health, unapproved, and other-event data are not queried.
- Browser validation using the authenticated participant session confirmed `/dashboard/workspaces/preferences` renders its participant/organizer choices, disabled no-grant capability state, account controls, safe explanation, and All workspaces link without overlap; no preference was submitted. The active CSR lifecycle was verified database-first with a temporary isolated account to avoid changing retained user data. The focused test created an active selected-function grant, saved a capability landing preference, created a profile/budget/request, submitted it, completed MASTER approval, created a concept assignment, rejected concept-only funding, confirmed zero participant exposure, and removed every temporary record.
- Flag coverage verifies default-enabled/exact-`false` rollback behavior for workspace landing preferences, grant usage reporting, and Stage 8 CSR capability routing. Setting the CSR Stage 8 flag to exact `false` removes its operational workspace while leaving legacy `/csr`, grants, preferences, requests, sponsorships, assignments, concepts, and audits intact.
- Final validation passed with **42 Vitest files / 102 tests**, TypeScript, and the bounded-memory Next.js 16.3.1 production build. Focused cleanup verification returned `stage8_users=0;stage8_grants=0;stage8_preferences=0;csr_request_count=0;csr_sponsorship_count=0;csr_assignment_count=0;csr_concept_count=0`. Final preservation verification reported `users=5;profiles=5;roles=admin:1,csr:1,mcd:1,user:2;catalog=6;activeGrants=0;workspacePreferences=0;stage8Requests=0;stage8Sponsorships=0;stage8Assignments=0;stage8Concepts=0;stage8TestUsers=0`.
- The inspected `fitizen-india-rebuild-nextjs-stage8-csr-capability.zip` archive contains **372** source entries and is 690,276 bytes. It includes `package.json`, `SETUP.md`, migrations `0031`/`0032`, the Stage 8 inventory, and the Stage 8 CSR workspace component. Archive inspection found zero forbidden dependency, build-output, legacy Vite client/server/shared source, log, secret, metadata, local database, or Vite-config entries.

- **Requested CSR administration enhancements and Stage 9 authority capability workspaces:** Administrators can now download CSV/XLSX CSR grant-usage reports from Capability Governance. The server-derived report contains only current grant state, selected functions, validity window, request/assignment/concept/funding aggregate counts, execution count, and latest execution timestamp. The route requires an administrator and excludes participant fields, payment detail, health data, and legacy CSR requests that are not capability-grant linked.
- A saved capability workspace default now emits an owner-scoped in-app alert when its exact active grant ends within 30 days. It is derived on request, does not persist a notification or invoke a scheduler/email, and cannot change authorization. Login continues to revalidate preferences and falls back safely for expired, suspended, foreign, inactive, or functionless grants. The administrator CSR view now includes a read-only capability assignment timeline from request, review, assignment, funding, and report timestamps; it has no mutation controls and no participant fields.
- Migration `0033_dark_bloodstrike.sql` additively creates grant-linked `authorityDeliveryPlans`, `authorityStateProgrammes`, and `authorityExceptions`, then idempotently seeds only operational Stage 9 authority functions. It leaves existing users, roles, credentials, sessions, events, venues, legacy Local Authority workflow, legacy CSR workflow, historic requests, grants, and audits unchanged.
- The Stage 9 Local Authority/District-Level/State-Level dynamic workspace revalidates the exact account-owned active grant, capability, selected function, date interval, catalogue state, and resource territory on every route and action. District plans, state programmes, and authority exception/escalation records inherit only their exact grant scope. Local Authority/District event review reuses the retained lifecycle transition under exact selected-function scope. MIS exports are aggregate/masked. Stage 10 health features were not introduced: no health form, record query, participant-level authority export, or health export exists.
- Focused Stage 9 regression created a temporary standard user with independent city-scoped District-Level and state-scoped State-Level grants, verified saved-default expiry alert derivation, plan creation, exception creation/resolution, separate state programme creation, wrong-capability denial, exact workspace context, and cleanup. It passed together with flag regressions. The complete suite passed with **43 Vitest files / 104 tests**, and TypeScript plus the bounded-memory Next.js 16.3.1 production build passed. Browser navigation to the protected preference route without a retained authenticated session correctly returned a blank protected shell; this was recorded as an authentication limitation, not a visual product failure.
- Final focused cleanup verification returned `stage9Users=0;stage9Preferences=0;stage9Grants=0;stage9Plans=0;stage9Programmes=0;stage9Exceptions=0;stage9Audit=0`. Final preservation verification reported `users=5;profiles=5;roles=admin:1,csr:1,mcd:1,user:2;catalog=6;functions=27;activeGrants=0;workspacePreferences=0;authorityPlans=0;authorityProgrammes=0;authorityExceptions=0;stage9Users=0;stage9Grants=0`.
- The inspected `fitizen-india-rebuild-nextjs-stage9-authority-capabilities.zip` archive contains **381** source entries and is 722,178 bytes. It includes `package.json`, `SETUP.md`, migration `0033_dark_bloodstrike.sql`, the Stage 9 inventory, authority workspace component, and both authority/CSR export routes. Archive exclusion verification found no dependencies, Next.js build output, legacy Vite client/server/shared source, logs, secrets, Manus metadata, local database, or Vite-config entries.

- **Stage 10 — consented append-only participant history, aggregate authority analytics, and authority-grant reminders:** Migration `0034_talented_manta.sql` was reviewed and applied additively. It creates consent, append-only participant-history entry, correction, audit, and durable reminder-delivery tables; no existing events, registrations, categories, users, roles, grants, or historic audit rows were modified. The migration idempotently seeds only `LA_HEALTH_AGGREGATE_VIEW`, `DISTRICT_HEALTH_AGGREGATE_VIEW`, and `STATE_HEALTH_AGGREGATE_VIEW` selected functions.
- Confirmed participants can use `/dashboard/history` for server-derived category forms. Running/Wellness require explicit health consent and show voluntary wellbeing check-ins; Learning, Community, and Music/Food & Drink show distinct non-health reflection forms. The server validates registration owner, category, type, current consent, date inside the event range, and one entry per registration/type/date. Single-day events permit one entry; multi-day events permit dated daily updates. No browser-supplied category, scope, or registration claim is trusted.
- History entries are insert-only and receive `HEN-…` identifiers. A correction produces a distinct `HCR-…` record with a reason and validated payload; the original row remains unchanged. Participant view, consent, creation, correction, aggregate authority view, and aggregate export operations append history audit evidence. Individual participant fields, raw payload, notes, corrections, and IDs are not queried by authority or CSR workspaces.
- Aggregate health charts and CSV export appear only to a current exact Local Authority, District-Level, or State-Level workspace grant that selects its matching health aggregate function. The common scope matcher limits counts to the grant territory. Charts and export contain only counts by date/status and do not include identity, registration, raw data, notes, or correction content. Foreign/expired/unselected-grant access is denied.
- Stage 10 implements both selected reminder modes. The dashboard derives owner-scoped authority-grant warnings within 30 days. The protected cron route `/api/scheduled/authority-grant-reminders` uses scheduled authentication and durable `(grant, recipient, reminder-window)` idempotency to create at most one private notice for each 30/14/7/3/1-day window; it never auto-renews or changes a grant. After explicit publication confirmation, enabled project job `authority-grant-reminders-daily` (`oJmcXfBhsJKyF4NkrALEBP`) was created for 09:00 UTC daily delivery. Configuration verification found the protected POST path, active status, and no prior runs before its first scheduled window.
- Homepage discovery now uses normal query-preserving GET forms and quick-filter links, so a Free filter renders `/?filter=Free&sort=soonest` from server state. The hero carousel uses ordinary previous/next/dot links with a `banner` query value rather than a client hydration-dependent timer; live verification confirmed `?banner=1` renders Banner 2 and a Next click reaches `?banner=2` with distinct content and visual treatment. These controls remain keyboard reachable and preserve discovery filters.
- The focused Stage 10 lifecycle test verified consent denial before entry creation, consent success, a first immutable health entry, duplicate daily-entry rejection, separate correction lineage with an unchanged original payload, exact selected-function aggregate authority access, foreign-user denial, in-app expiry query, and idempotent reminder delivery. Cleanup verification returned `stage10Users=0;stage10Consents=0;stage10Entries=0;stage10Corrections=0;stage10HistoryAudits=0;stage10ReminderDeliveries=0;stage10Grants=0`.
- Final code validation passed **44 Vitest files / 106 tests**, TypeScript, and the bounded-memory Next.js 16.3.1 production build. The build includes `/dashboard/history`, `/api/exports/authority-health`, and `/api/scheduled/authority-grant-reminders` as dynamic routes.
- The inspected `fitizen-india-rebuild-nextjs-stage10-health-history.zip` archive contains **393** source entries and is 757,234 bytes. It includes `package.json`, `SETUP.md`, migration `0034_talented_manta.sql`, the Stage 10 inventory, the participant-history workspace, and the scheduled authority-reminder handler. Archive exclusion verification found no dependencies, Next.js build output, legacy Vite client/server/shared source, Vite configuration, logs, secrets, Manus metadata, or local database entries.
- The event-creation Description step now uses a native required `textarea` named `description` as the canonical submitted field. This replaces a hydration-dependent `contentEditable` surface whose hidden mirror could remain empty, causing the observed 20-character validation redirect and discarded visible text. The server still validates the minimum length, persists the submitted description, and advances the event to step 4. Focused regression coverage, the full 47-file/110-test suite, TypeScript, and the bounded-memory production build passed.
- Event-banner saving now posts the organizer-selected image file with the wizard form, validates its JPEG/PNG/WebP MIME, 5 MB limit, and byte signature server-side, then stores only the durable storage URL on the owned event record. Registration QR image and poster endpoints resolve the event from the path and deny draft/unapproved records before generating any output. Live verification of current draft `noida-10km-run-mt874sph` returned **“Registration QR is available after administrator approval.”** The public homepage also verified smooth query-backed Free filtering and Banner 2 navigation while preserving all active discovery parameters.
- The Cover Image step now provides a keyboard-accessible drag-and-drop target and native file-picker fallback. A selected JPEG, PNG, or WebP first opens the existing 16:9 cropper with zoom and position controls, then returns a cropped WebP file to the same multipart `coverFile` submission path. The server retains ownership, MIME, 5 MB, and byte-signature validation; no browser URL is trusted.
- The Tickets and Pricing step now has explicit clickable Paid, Free, and Donation cards. Paid selection enables a required decimal-capable price field and displays immediate validation; Free disables pricing; Donation supports an optional suggested amount. New tickets derive a populated sale period from event timing, while the server rejects any paid ticket with a zero price and any invalid sales window.
- Organizers now receive a read-only approval timeline within Create Event. It is owner-scoped and derives draft creation, submission, current review status, and historic administrator/Local Authority decisions from the existing event timestamps and administrator audit records. It displays administrator guidance already intended for the organizer and does not alter MASTER or Local Authority authorization.
- The existing selected-venue calendar and time-slot layout is preserved. A failed reservation attempt now returns to that venue’s calendar in directory mode, while the booking selector remains protected against selecting it. The calendar selector can inspect reserved venues, immediately focuses the holding event’s date, colors reserved dates and only their overlapping two-hour time windows red, and leaves all other date/time cells green. Date and slot tooltips show the holding event and its actual time range from the same server-backed reservation records used for the conflict warning.
- The calendar now receives that same active reservation list during the initial authenticated wizard render instead of waiting for the optional client availability request. Therefore, selecting or inspecting a venue shows its red reserved dates/time slots and green available slots immediately; the client route remains a follow-up refresh, not the only source of calendar data.
- The calendar keeps its existing grid and timeline treatment: only timeline cells that overlap a reservation are red, while available cells are green. When every listed time window on a selected day is reserved, the existing timeline guidance names the next available date and time. A compact inline upcoming-reservations line below the existing legend shows at most three future holding events and time ranges rather than creating a second reservation panel.
- Participant registration and Following queries now reject actual organizer draft events and deleted events at the database boundary. Organizer dashboards retain their existing draft records and workflow access; live legacy events remain visible to participants even where historic moderation metadata is incomplete.

- User-authorized scoped cleanup removed only the four participant-demo Noida 10KM Run drafts: IDs `2850001`, `2910001`, `3000001`, and `3060001`. Before deletion, each was confirmed under `participant@fitizen.local` (user `180001`) with both lifecycle fields set to `draft`; all had zero child tickets, questions, registrations, follows, and non-cascading venue/CSR/history/promotion references. The guarded deletion required each exact ID, the owner, and both draft states. Post-delete checks found zero selected event or child rows, preserved the participant account, and preserved two unrelated events owned by `demo@fitizen.local`.

- The Create Event ticket payment selector now uses one native, controlled radio group rather than independent card buttons. Choosing Paid, Free, or Donation updates the ticket type in one state transition; Paid and Donation immediately enable the amount input, while selecting Free clears price and GST state. Regression coverage includes Free → Paid, Paid → Donation, and Paid → Free transitions. Full validation passed: 52 Vitest files / 126 tests, TypeScript, and the bounded-memory production build.

- Ticket payment flow remediation: the real browser check found the previous client-only ticket and manual-payment controls were not attached in the actual organizer wizard, so selecting Paid could not update the saved form. The ticket composer and manual-payment controls now use native inputs plus CSS-selected Paid, Free, and Donation price fields; the server action accepts a native ticket payload alongside existing saved tickets. Sale defaults close one minute before the event start. In an authenticated organizer browser flow, Paid exposed an editable price field, manual UPI settings opened, and `Temporary Paid Ticket` saved at ₹499 / 49,900 paise to step 6. The isolated test event `3330001` and ticket were then deleted; the organizer demo account remained. The organizer My Events page now has visible entry points for CSR, Local Authority, and the complete approved capability catalog, each using the retained time-bound master-review workflow. Full validation passed: 54 Vitest files / 130 tests, TypeScript, and bounded-memory production build.

- Native pricing controls now keep GST rate unavailable while GST is No, send a forced 0% rate in that state, and offer 0%, 5%, 12%, 18%, and 28% when GST is Yes. Manual-payment settings now use native UPI-only, Bank-only, and Both radios; only the selected method’s detail fields render, including before browser hydration. The buyer registration panel now presents ticket price, GST, buyer-borne platform fee, Fitizen and gateway fee-allocation disclosures, and a server-aligned total payable; it never invents a Fitizen or gateway amount where no configured rate exists. Full validation passed: 56 Vitest files / 133 tests, TypeScript, and bounded-memory production build.

- Post-stage read-only preservation audit found five accounts using only the retained `user`, `admin`, `mcd`, and `csr` role values; 36 event records, 6 tickets, 7 registrations, 1 follow, and 238 administrator audit rows; and zero orphan tickets, registrations, follows, or temporary ticket-selector event `3330001`. The stage capability catalog contains six additive definitions with no application, grant, or capability-audit records. Four older organizer-owned verification-named drafts from 2026-08-21 remain preserved and were not deleted. This confirms current relational integrity; an after-the-fact database cannot prove a byte-for-byte pre-stage snapshot, but the documented migrations add models/metadata and no unintended role overwrite or broken relationship was detected.

- Homepage database-query repair: the reported failure was not schema drift—the live `events` table contains all 53 columns selected by `listPublicEvents`. Runtime evidence identified a stale MySQL connection (`PROTOCOL_CONNECTION_LOST`, with later `ECONNRESET` records). The existing singleton now uses an explicitly configured keep-alive pool, and the public homepage read retries once only for transient connection failures; writes and non-connection errors are never retried. The homepage rendered successfully with its existing discovery layout and event images. Non-mutating regression coverage passed: 42 files / 110 tests, including the new connection-recovery tests; TypeScript and bounded-memory production build passed. No database records were created, deleted, or updated.

import { MapPin } from "lucide-react";
import { adminCreateDistrictAccountAction } from "../actions";

export default function AdminDistrictProvisioner() {
  return <section className="panel" aria-labelledby="district-provision-title"><div className="section-head"><div><span className="eyebrow"><MapPin size={13} /> District administration</span><h3 id="district-provision-title">Create District Authority account</h3><p>District Authority accounts receive read-only platform access scoped to their role. They cannot change events, users, payments, or the venue directory — those remain master-administrator-only. Creation requires MASTER and is recorded in the audit log.</p></div></div><form action={adminCreateDistrictAccountAction} className="venue-editor"><div className="two-col"><label className="form-label">Authority contact name *<input className="input" name="name" required minLength={2} placeholder="e.g. District Public Health Desk" /></label><label className="form-label">Official email *<input className="input" name="email" type="email" required placeholder="district.authority@example.gov.in" /></label></div><div className="two-col"><label className="form-label">Initial password *<input className="input" name="password" type="password" required minLength={8} autoComplete="new-password" /></label><label className="form-label">Confirmation *<input className="input" name="confirmation" required placeholder="Type MASTER" /></label></div><button type="submit" className="btn btn-coral">Create District Authority account</button></form></section>;
}
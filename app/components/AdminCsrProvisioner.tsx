import { Building2 } from "lucide-react";
import { adminCreateCsrAccountAction } from "../actions";

export default function AdminCsrProvisioner() {
  return <section className="panel" aria-labelledby="csr-provision-title"><div className="section-head"><div><span className="eyebrow"><Building2 size={13} /> Corporate social responsibility</span><h3 id="csr-provision-title">Create CSR sponsor account</h3><p>A CSR sponsor is a funding role, not an event-organiser role. Master administrators create each company account with a linked company profile and an audited MASTER confirmation.</p></div></div><form action={adminCreateCsrAccountAction} className="venue-editor"><div className="two-col"><label className="form-label">Account contact name *<input className="input" name="name" required minLength={2} placeholder="e.g. CSR programme lead" /></label><label className="form-label">Account email *<input className="input" name="email" type="email" required placeholder="csr.account@company.example" /></label></div><div className="two-col"><label className="form-label">Initial password *<input className="input" name="password" type="password" required minLength={8} autoComplete="new-password" /></label><label className="form-label">Company legal name *<input className="input" name="companyName" required minLength={2} placeholder="e.g. Example Foundation" /></label></div><div className="two-col"><label className="form-label">Company registration number<input className="input" name="registrationNumber" placeholder="Optional registration reference" /></label><label className="form-label">Foundation or CSR brand<input className="input" name="foundationName" placeholder="Optional programme or foundation name" /></label></div><div className="two-col"><label className="form-label">CSR profile contact *<input className="input" name="contactName" required minLength={2} placeholder="Named accountable contact" /></label><label className="form-label">CSR profile email *<input className="input" name="contactEmail" type="email" required placeholder="csr.team@company.example" /></label></div><div className="two-col"><label className="form-label">Phone<input className="input" name="contactPhone" placeholder="Optional contact number" /></label><label className="form-label">MASTER confirmation *<input className="input" name="confirmation" required placeholder="Type MASTER" /></label></div><label className="form-label">CSR focus areas<textarea className="input textarea" name="focusAreas" rows={3} placeholder="e.g. youth fitness, accessible community activity, ward-level wellness" /></label><button type="submit" className="btn btn-coral">Create CSR sponsor account</button></form></section>;
}

import { Landmark } from "lucide-react";
import { adminCreateLocalAuthorityAccountAction } from "../actions";

export default function AdminMcdProvisioner() {
  return <section className="panel" aria-labelledby="local-authority-provision-title"><div className="section-head"><div><span className="eyebrow"><Landmark size={13} /> Public-health authority</span><h3 id="local-authority-provision-title">Create Local Authority account</h3><p>This authority access can monitor public-health activity and record only event lifecycle reviews. User, payment, and venue-directory changes remain master-administrator-only. Creation requires MASTER and is recorded in the audit log.</p></div></div><form action={adminCreateLocalAuthorityAccountAction} className="venue-editor"><div className="two-col"><label className="form-label">Authority contact name *<input className="input" name="name" required minLength={2} placeholder="e.g. Local Public Health Desk" /></label><label className="form-label">Official email *<input className="input" name="email" type="email" required placeholder="local.authority@example.gov.in" /></label></div><div className="two-col"><label className="form-label">Initial password *<input className="input" name="password" type="password" required minLength={8} autoComplete="new-password" /></label><label className="form-label">Confirmation *<input className="input" name="confirmation" required placeholder="Type MASTER" /></label></div><button type="submit" className="btn btn-coral">Create Local Authority account</button></form></section>;
}
